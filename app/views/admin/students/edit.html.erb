<h1>Editing Student</h1>

<%= render 'form', student: @student, path: admin_student_path(@student) %>

<%= link_to 'Show', admin_student_path(@student) %> |
<%= link_to 'Back', admin_students_path %>
