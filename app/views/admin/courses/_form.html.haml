= form_with(model: course, url: path, local: true) do |f|
  - if course.errors.any?
    #error_explanation
      %h2= "#{pluralize(course.errors.count, "error")} prohibited this course from being saved:"
      %ul
        - course.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :name
    = f.text_field :name

  = f.submit
