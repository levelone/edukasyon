<h1>Editing Teacher</h1>

<%= render 'form', teacher: @teacher, path: admin_teacher_path(@teacher) %>

<%= link_to 'Show', admin_teacher_path(@teacher) %> |
<%= link_to 'Back', admin_teachers_path %>
