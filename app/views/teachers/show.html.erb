<p>
  <strong>Teacher:</strong>
  <%= @teacher.full_name %>
</p>

<p>
  <strong>Email:</strong>
  <%= @teacher.email %>
</p>

<p>
  <strong>Courses:</strong>
  <ul>
    <% @teacher.klasses.offered_klasses.each do |klass| %>
      <li><%= link_to klass.course.name, klass_path(klass, redirect_to: URI.encode(teacher_path(@teacher))) %></li>
    <% end %>
  </ul>
</p>


<%= link_to 'Back', teachers_path %>
